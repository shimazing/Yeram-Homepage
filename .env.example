# Site URL (used for SEO and social media sharing)
VITE_SITE_URL=https://www.yeram.me.kr

# Upload Directory (for gallery images)
# For Railway deployment with Volume: /data/uploads
# For local development: leave empty to use ./attached_assets
# UPLOAD_DIR=/data/uploads

# Node Environment
NODE_ENV=development

# Database Configuration (Optional)
# Leave DATABASE_URL empty to use FileStorage (data.json)
# DATABASE_URL=postgresql://user:password@host:port/database

# Data File Path (for FileStorage)
# For Railway deployment with Volume, set this to /data/data.json
# For local development, leave empty to use ./data.json
# DATA_FILE_PATH=/data/data.json
